<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Plasma Hall of Fame</title>
  <style>
    body { font-family: Arial, sans-serif; text-align: center; background: black; color: white; }
    h1 { margin-top: 20px; }
    #wall { display: flex; flex-wrap: wrap; justify-content: center; margin-top: 20px; }
    .pfp { width: 100px; height: 100px; border-radius: 50%; margin: 10px; border: 2px solid #ff0077; }
    input { padding: 10px; font-size: 16px; }
    button { padding: 10px; font-size: 16px; background: #ff0077; color: white; border: none; cursor: pointer; }
    button:hover { background: #cc0066; }
  </style>
</head>
<body>
  <h1>🔥 Plasma Hall of Fame 🔥</h1>
  <p>Enter your Twitter username to join the wall</p>
  <input type="text" id="username" placeholder="e.g. jack">
  <button onclick="addToWall()">Add</button>

  <div id="wall"></div>

  <script>
    function addToWall() {
      const user = document.getElementById('username').value.trim();
      if (!user) return;
      const img = document.createElement('img');
      img.src = `https://unavatar.io/twitter/${user}`;
      img.className = "pfp";
      document.getElementById('wall').appendChild(img);
      document.getElementById('username').value = "";
    }
  </script>
</body>
</html>
